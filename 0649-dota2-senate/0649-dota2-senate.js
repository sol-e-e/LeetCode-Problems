/**
 * @param {string} senate
 * @return {string}
 */
var predictPartyVictory = function(senate) {
    const queue = senate.split('');
    let rl = 0;
    let dl = 0;

 // DRRDRDRDRDDRDRDR
 // DRRDRDRDRDDRDRDR
  // DRRDRDRDRDDRDRDR
    while (queue.length > 1) {

    }
};